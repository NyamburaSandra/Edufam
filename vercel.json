{
  "version": 2,
  "buildCommand": "cd Frontend/EDUFAM && npm install && npm run build",
  "outputDirectory": "Frontend/EDUFAM/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}